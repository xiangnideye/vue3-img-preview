import type { App } from 'vue';
import { Preview } from './preview/index'

declare const install: (app: App) => void;
export { install, Preview };
declare const _default: {
  install: (app: App<any>) => void;
};
export default _default;
